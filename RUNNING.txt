You probably want to use 'screen' to run the bank/atm/proxy code

We're using per-user keys stored in .card files. We assume that a malicous user would have access to their .card file but not those of other users (since the physical atm is hypothetically tamper proof).
