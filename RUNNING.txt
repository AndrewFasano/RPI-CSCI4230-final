You probably want to use 'screen' to run the bank/atm/proxy code

Our keys are stored in the .card files. We are assuming an attacker would not have access to these as they would be stored on the tamper-proof ATM. Since they're stored seperately from our binaries, we can't remove them from the code we're providing you.
